import { DropdownHighlightDirective } from './dropdown-highlight.directive';

describe('DropdownHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new DropdownHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
